# ============================================================================
# AWS Secrets Manager Configuration (Optional)
# ============================================================================
# By default, the seeder will attempt to fetch secrets from AWS Secrets Manager
# and fallback to .env files if AWS is unavailable or disabled.
#
# Set USE_AWS_SECRETS=false to disable AWS secrets and use .env files only.
# AWS_REGION defaults to us-east-1 if not specified.
# IMPORTANT: Verify your secrets' region by checking the ARN in AWS Secrets Manager.
# The region in the ARN (e.g., us-east-2) must match AWS_REGION.
#
# Secret names can be customized via:
# - AWS_DATABASE_SECRET_NAME (default: dev/uat-database-url)
# - AWS_SHOPIFY_SECRET_NAME (default: dev/shopify-access-token)
#
# AWS credentials are automatically detected from:
# 1. Environment variables (AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY)
# 2. AWS IAM role (when running in AWS)
# 3. AWS credentials file (~/.aws/credentials) - use AWS_PROFILE to select profile
# 4. Default credential provider chain
#
# If you have multiple profiles in ~/.aws/credentials, set AWS_PROFILE to the
# profile name you want to use (e.g., "dev", "staging", "production").
# If AWS_PROFILE is not set, the "default" profile will be used.
# ============================================================================

# USE_AWS_SECRETS=true
# AWS_REGION=us-east-2
# AWS_PROFILE=default
# AWS_DATABASE_SECRET_NAME=dev/uat-database-url
# AWS_SHOPIFY_SECRET_NAME=dev/shopify-access-token

# ============================================================================
# Environment Variables (Fallback when AWS secrets are unavailable)
# ============================================================================
# These values are used when:
# - USE_AWS_SECRETS=false
# - AWS secrets are unavailable or fail to fetch
# - Values are missing from AWS secrets (partial fallback)

# Database connection string (must match staging patterns: staging, stage, test, dev, uat)
# Example: postgresql://user:password@staging-db.example.com:5432/wms_staging
DATABASE_URL=postgresql://user:password@staging-db.example.com:5432/wms_staging

# Database connection pool limit (optional, defaults to 10)
# Controls maximum number of concurrent database connections
# Increase for parallel operations (e.g., 20 for 10+ concurrent collection prep generations)
# Example: DATABASE_CONNECTION_LIMIT=20
# DATABASE_CONNECTION_LIMIT=10

# Shopify store domain (must match staging patterns: staging, stage, test, dev, uat, or .myshopify.com)
# Example: staging-store.myshopify.com
# This is used for CA region (default)
SHOPIFY_STORE_DOMAIN=staging-store.myshopify.com

# Shopify Admin API access token
# Get this from your Shopify admin panel: Settings > Apps and sales channels > Develop apps
# Example: shpat_xxxxxxxxxxxxx
# This is used for CA region (default)
SHOPIFY_ACCESS_TOKEN=shpat_xxxxxxxxxxxxx

# US Shopify store domain (optional - only needed for US region support)
# Example: us-staging-store.myshopify.com
# SHOPIFY_STORE_DOMAIN_US=us-staging-store.myshopify.com

# US Shopify Admin API access token (optional - only needed for US region support)
# Get this from your US Shopify admin panel: Settings > Apps and sales channels > Develop apps
# Example: shpat_xxxxxxxxxxxxx
# SHOPIFY_ACCESS_TOKEN_US=shpat_xxxxxxxxxxxxx

# Shopify API version (optional, defaults to 2024-01)
# Check Shopify API documentation for latest stable version
SHOPIFY_API_VERSION=2024-01
